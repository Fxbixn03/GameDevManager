@page "/modules/{ModuleId}"
@inject IStringLocalizer<ModulePage> L
@inject ModuleLabels Modules

@if (_module is null)
{
    <PageTitle>@L["UnknownPageTitle"]</PageTitle>
    <MudAlert Severity="Severity.Error">@L["UnknownModuleMessage", ModuleId]</MudAlert>
}
else
{
    <PageTitle>@L["PageTitleFormat", Modules.Name(_module)]</PageTitle>

    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="3" Class="mb-4">
        <MudIcon Icon="@_module.Icon" Size="Size.Large" />
        <MudText Typo="Typo.h4">@Modules.Name(_module)</MudText>
    </MudStack>

    <MudText Typo="Typo.body1" Class="mb-6">@Modules.Description(_module)</MudText>

    <MudAlert Severity="Severity.Info">
        @L["InDevelopmentHint"]
    </MudAlert>
}

@code {
    [Parameter]
    public string ModuleId { get; set; } = "";

    private ModuleDefinition? _module;

    protected override void OnParametersSet() =>
        _module = ModuleRegistry.Find(ModuleId);
}
